const StreamErrorEnum = Object.freeze({
  MAX_CLIENTS: 'max_clients',
  BLACKLIST: 'blacklist',
  INVALID_MODE: 'invalid_mode',
  INVALID_STREAMID: 'invalid_streamid',
})

export default StreamErrorEnum
