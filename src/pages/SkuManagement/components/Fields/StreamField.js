import React from 'react'
import { CircularProgress } from 'ambient_ui'

const StreamsField = ({ rowData, darkMode }) => {
  const { node } = rowData

  return <div>10 streams</div>
}

export default StreamsField
