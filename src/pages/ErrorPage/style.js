import { makeStyles } from '@material-ui/core/styles'

export default makeStyles({
  container: {
    padding: 0,
    margin: 0,
    backgroundColor: '#222428',
  },
  wrapper: {
    height: '100%',
  },
})
