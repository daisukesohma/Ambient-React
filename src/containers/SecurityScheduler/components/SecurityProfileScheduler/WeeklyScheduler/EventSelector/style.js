import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
  formGroup: {
    flexDirection: 'row',
    justifyContent: 'flex-start',
    paddingLeft: 10,
  },
  formLabel: {
    fontSize: 16,
  },
}))
