/* TODO(AMB-2276|@rys): create AmbientModal and reuse it for modals you created for SOC Recall project.
ex) here are some ideas.
AmbientModal:
    props:
        title: string
        children: React.Elements (so that we can pass anything)
        onCancel: function?
        onSubmit: function?
        submitText: string? default: Submit

*/
const AmbientModal = (): null => {
  return null
}
export default AmbientModal
